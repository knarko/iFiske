/**
 * Debug Object for finding Debug lines easier
 * @class Debug
 */
Debug = Object.freeze({
    /**
     * Logs to console using console.log
     * @method log
     * @param {Any} err Anything that can be logged
     */
    log: function(err) {
	console.log(err);
    }
});
